import type { Metadata } from 'next';
import { ThemeProvider } from '@sidequestteams/mission-control-sdk/theme';
import './globals.css';

export const metadata: Metadata = {
  title: '{{teamName}} — Mission Control',
  description: 'Dashboard for {{teamName}}',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" data-theme="dark" suppressHydrationWarning>
      <body>
        <ThemeProvider>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
